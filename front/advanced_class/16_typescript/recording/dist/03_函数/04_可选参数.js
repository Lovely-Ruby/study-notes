// // 加个问号即可
// function css(el: HTMLElement, attr: string, value?: any) {}
// let div = document.querySelector('div');
// div && css(div, 'color'); // 获取属性
// div && css(div, 'color', 'red'); // 设置属性
