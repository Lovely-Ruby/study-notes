// 空、未定义
// var a: null;
// var b: undefined;

// // 场景1：
// // a = 1

// // 场景2：
// // 默认情况下 `null` 和 `undefined` 是所有类型的子类型。 就是说你可以把 `null` 和 `undefined` 赋值给其它类型的变量
// var str1: string = 'i m string1';
// str1 = null;

// // 场景3：
// // 默认的值是 undefined，默认的类型是 any
// var c: number;
// console.log('c:>>', c);
// var d;

// let e: number;
// e = null;
// e.toFixed(2); // 实际是有问题的，但是 ide 没有报错

// var ele = document.querySelector('div');
// // ele.style.display = 'none';

// if (ele) {
//   ele.style.display = 'none';
// }
